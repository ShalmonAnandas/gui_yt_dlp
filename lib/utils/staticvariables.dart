import 'dart:io';

import 'package:flutter/material.dart';

class StaticVar {
  static Brightness darkTheme = Brightness.dark;
  static Brightness lightTheme = Brightness.light;

  static Color submitColor = const Color.fromRGBO(39, 136, 41, 1.000);
}
